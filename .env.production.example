# Production Environment Configuration

# Node environment (should be 'production' for production deployments)
NODE_ENV=production

# Server port (default: 3000)
# NOTE: Cloud platforms (Railway, Render, Fly.io) auto-inject PORT
# You typically don't need to set this manually for cloud deployments
# PORT=3000
